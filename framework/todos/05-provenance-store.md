# TODO 05 — Append-Only Provenance Store

> Status: done
> Spec: COMMUNITY_FRAMEWORK.md → "Append-only store" · PROVENANCE_SPEC.md (record format, hashing, corrections)
> Depends on: 01 (scaffold), 02 (config/identity)
> Blocks: 03, 04, 06, 10

## Goal

The permanent record layer: a store that accepts valid PROVENANCE_SPEC v0.1 records, preserves them append-only, serves them back, and can prove their integrity. When this section is done, a record written today is readable, verifiable, and unmodifiable by any code path in the package.

## Tasks

- [x] Django model for `ProvenanceRecord` mirroring PROVENANCE_SPEC v0.1 fields exactly (field names are spec-permanent: `hi_tag`, `provenancier`, etc.) — `store/models.py`
- [x] Canonicalization function: record JSON with `record_id` and `record_signature` set to `""`, keys sorted alphabetically, no whitespace (per spec Implementation Notes) — `betat_community/common/hashing.py` (moved out of `store/` per BLUEPRINT §0 Decision Log "API structure": shared with federation/workflow, so it belongs in `common/`, not one app)
- [x] `record_id` = SHA-256 of canonical form; computed at append, never accepted from the caller
- [x] `append(record)` — validates against spec (reject if `hi_tag` is not `true`; reject missing required fields; also rejects `content.type` ≠ `community.content_type`), computes hash, inserts
- [x] `get(record_id)` / `list_records(since, page)` — pagination newest-first (named `list_records` to avoid shadowing the `list` builtin)
- [x] `verify_integrity(record_id)` — recompute hash, compare, return boolean
- [x] `correction_of` / `disputes` reference fields accepted as new records only — modeled as self-referential FKs (`to_field='record_id'`) so a correction/dispute must reference an existing record
- [x] SQLite guard triggers: `BEFORE UPDATE` and `BEFORE DELETE` on the records table → `RAISE(ABORT)` — `store/migrations/0002_guard_triggers.py` (hand-authored `RunPython`, since triggers aren't autogenerated by `makemigrations`; written directly at developer's request after manual copy-paste attempts didn't land)
- [x] No `update()` / `delete()` methods anywhere in the store class — deliberate absence, enforced by test. Also added defense-in-depth beyond what this task asked: `AppendOnlyQuerySet.update()/delete()` and `ProvenanceRecord.delete()` all raise `NotSupportedError` at the ORM level, on top of the DB trigger
- [ ] PostgreSQL notes stub for TODO 12: role grant script (INSERT/SELECT only; UPDATE/DELETE revoked) — not started, correctly out of scope until §12

## Acceptance criteria

- [x] A spec-valid record appends and reads back byte-identical
- [x] `verify_integrity` passes on clean records; fails after any manual byte tampering in a test
- [x] A record with `hi_tag: false` or missing is rejected
- [x] Raw SQL `UPDATE`/`DELETE` against the table fails on SQLite (trigger) — `test_raw_sql_update/delete_blocked_by_sqlite_trigger` pass
- [ ] Acceptance test steps 5 and 6 (store half) — deferred to TODO 10

## Tests
- `framework/tests/test_store.py`: 16 tests. 22/22 total (store + core) passing, developer-verified.

## Security notes

- The hash is computed server-side, always — a caller-supplied `record_id` is an attack surface
- Canonicalization must be deterministic across Python versions (use `json.dumps(..., sort_keys=True, separators=(",",":"))`)
- Triggers are defense-in-depth, not the security boundary; the spec's honest position (app-level + triggers on SQLite, role revocation on PostgreSQL) is documented in COMMUNITY_FRAMEWORK.md and must not be overstated in docs
- **Dual-DB seam (per BLUEPRINT §0):** this is the ONE place engine-specific code is sanctioned. The store's behavior (append, read, verify, no-update/delete) must test identically on SQLite and PostgreSQL, even though enforcement differs (triggers vs role revocation). Keep everything else ORM-based so the store is engine-agnostic outside this seam.

## Out of scope for this section

- Federation endpoints (06) — the store is storage, not transport
- Record building from submissions (04) — the store validates and keeps; it does not compose
- PostgreSQL setup itself (12) — only the grant-script stub lands here
